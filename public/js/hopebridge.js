// public/js/hopebridge.js

document.addEventListener('DOMContentLoaded', function() {
    console.log("HopeBridge custom JS loaded!");

    // Add any global JavaScript functions or event listeners here.
    // For example, a simple scroll-to-top button logic could go here,
    // or general utility functions.

    // Initialize Bootstrap tooltips and popovers if you use them
    // var tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'))
    // var tooltipList = tooltipTriggerList.map(function (tooltipTriggerEl) {
    //     return new bootstrap.Tooltip(tooltipTriggerEl)
    // });
    // var popoverTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="popover"]'))
    // var popoverList = popoverTriggerList.map(function (popoverTriggerEl) {
    //     return new bootstrap.Popover(popoverTriggerEl)
    // });
});